/**
 * Иконки проекта.
 * - ChinorLogo — бренд (шеф-шапка), использовать в логотипе и фавиконе.
 * - Остальные иконки — из lucide-react (уже в зависимостях), импортировать напрямую:
 *   import { LayoutDashboard, Calendar, Users, Send, Settings } from 'lucide-react'
 */
export { ChinorLogo } from './chinor-logo'
